# LinkCaptcha RPA 应用使用说明

## 1. 应用用途

LinkCaptcha RPA 用于辅助完成一个固定的微信桌面端操作流程：

1. 在当前微信聊天窗口中识别底部最新的蓝色链接。
2. 点击该链接。
3. 在打开的验证码页面中识别四位数字验证码。
4. 点击验证码输入框并填写验证码。
5. 点击提交按钮。

应用通过屏幕截图、图像识别和鼠标键盘模拟完成操作。它不读取微信本地数据库，不修改微信客户端，不注入微信进程。

## 2. 文件说明

分发给使用方时，应提供完整文件夹或 zip 包。

解压后的结构类似：

```text
WeChatLinkCaptchaRPA/
  WeChatLinkCaptchaRPA.exe
  _internal/
    ...
```

注意：

- `WeChatLinkCaptchaRPA.exe` 是启动程序。
- `_internal/` 是程序运行依赖。
- 不要只复制单独的 exe 文件，必须让 exe 和 `_internal/` 保持在同一个目录。

## 3. 启动

1. 解压分发包。
2. 打开 `WeChatLinkCaptchaRPA/` 文件夹。
3. 双击 `WeChatLinkCaptchaRPA.exe`。

启动后会出现：

- 一个小型“微信验证码 RPA 控制器”窗口。
- 四个置顶半透明校准框。

## 4. 四个校准框

### 链接框

蓝色框。

用于框住微信聊天窗口中最新链接所在区域。程序会在这个区域里找蓝色链接，并选择最靠下的候选。

建议：

- 覆盖最近几条消息即可。
- 不要框到联系人列表、输入框、其他蓝色按钮。
- 如果检测错误，缩小这个框通常最有效。

### 识别框

绿色框。

用于框住网页中的四位数字验证码。

建议：

- 只框验证码数字和少量边距。
- 不要框到输入框、按钮或说明文字。

### 填写框

黄色框。

程序会点击此框中心点，并输入识别出的四位验证码。

建议把中心点对准验证码输入框中间。

### 提交框

红色框。

程序会点击此框中心点提交。

建议把中心点对准提交按钮中间。

## 5. 控制器按钮

### 运行一次

执行完整流程：

```text
识别链接 -> 点击链接 -> 等待页面 -> 识别验证码 -> 填写 -> 提交
```

### 测试链接

只检测链接，不点击。日志会显示检测结果和坐标。

如果失败，日志会显示链接框真实像素坐标和蓝色像素数量，用来判断是框的位置问题还是颜色阈值问题。

### 点击链接

检测链接并点击，但不继续识别验证码。

### 测试验证码

只识别绿色识别框内的验证码，不点击、不提交。

### 取样链接色

用于适配不同微信主题或链接颜色。

操作：

1. 点击“取样链接色”。
2. 3 秒内把鼠标移动到真实蓝色链接文字上。
3. 程序自动读取鼠标位置颜色并生成 HSV 阈值。

### 显示/隐藏框

临时显示或隐藏四个校准框。

程序在截图和点击时会自动临时隐藏校准框，避免框线影响识别或挡住点击。

### 保存配置

保存当前四个框的位置、大小、链接颜色阈值和等待时间。

### 加载配置

加载已保存配置。

如果更换电脑、显示器、Windows 缩放比例、微信窗口位置或网页布局，建议重新校准并保存。

## 6. 推荐测试流程

先使用固定验证码测试页：

```text
https://koa-ol.com/captcha-test/?code=1234
```

步骤：

1. 把链接发到微信聊天中。
2. 打开应用。
3. 拖动蓝色链接框覆盖微信中的链接消息。
4. 点击“取样链接色”，把鼠标移到链接文字上等待取样。
5. 点击“测试链接”，确认能检测到链接。
6. 点击“点击链接”，打开验证码页面。
7. 拖动绿色识别框覆盖验证码数字。
8. 拖动黄色填写框覆盖输入框。
9. 拖动红色提交框覆盖提交按钮。
10. 点击“测试验证码”，确认识别结果。
11. 点击“保存配置”。
12. 返回微信，点击“运行一次”测试完整流程。

## 7. 常见问题

### 检测不到链接

可能原因：

- 链接框没有覆盖真实链接。
- Windows 缩放或显示器切换后未重新校准。
- 链接颜色与当前阈值不匹配。
- 框里有多个蓝色元素，候选被干扰。

处理：

- 缩小链接框，只框最新消息附近。
- 重新取样链接色。
- 重新拖动框并保存配置。

### 点击链接后没有打开微信浏览器

可能原因：

- 检测到的蓝色候选不是链接正文。
- 点击点落在链接边缘或空白处。
- 微信窗口未处于可点击状态。
- 同一个链接已经打开过，微信浏览器在后台。

处理：

- 使用“测试链接”查看日志中的候选坐标。
- 缩小链接框。
- 确保微信窗口可见且未被其他窗口遮挡。

### 验证码识别错误

当前轻量 OCR 适合清晰的四位数字验证码。对于强干扰、扭曲、粘连验证码，不保证高准确率。

处理：

- 缩小识别框，只框数字。
- 如果 OCR 失败，程序会弹出手动输入窗口。

### 坐标不准

程序已支持 Windows DPI 缩放转换。仍建议：

- 不要让一个框跨多个显示器。
- 更换显示器或缩放比例后重新校准。
- 远程桌面环境需要重新测试。

## 8. 使用边界

请只用于用户自己有权限访问的聊天和页面。不要用于批量提交、绕过风控、未经授权操作或违反平台规则的行为。

